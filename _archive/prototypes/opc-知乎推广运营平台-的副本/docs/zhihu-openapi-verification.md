# 知乎 OpenAPI 接入核验记录

## 已验证的真实资源

| 项目 | 标识 | 核验结果 |
| --- | --- | --- |
| 渠道 | `2067662706400834870` | 可通过开放平台读取渠道与任务。 |
| 推广任务 | `1443567656205545472` | 可通过签名接口读取，并已在当前渠道任务列表中匹配。 |
| 公开内容候选 | `https://www.zhihu.com/xen/market/remix/paid_column/2056100013286207953` | 知乎官方盐选专栏详情页，可公开访问，页面标识为单篇已完结内容。 |

## 使用边界

此候选仅用于用户已授权的一次 OpenAPI 创建验证。创建请求会使用用户提供的任务与渠道，并以本地幂等记录避免重复提交。知乎实时数据接口当前返回“接口未授权”，不得将其作为结算数据或自动刷新来源。

## 公开搜索核验

2026-08-15 在用户登录的知乎首页搜索“OPC 集成验证”。搜索页显示 28 篇包含 OPC、集成或验证等片段的内容，但未出现候选盐选内容《钝感力女配网恋后》或其 URL。该结果只能说明当前绑定词尚未被搜索索引为候选内容，**不能**用来确认外部计划已创建或已生效。随后输入候选标题，页面仅展示联想建议，尚未加载对应完整搜索页，因此未将其作为平台侧适配成功证据。

随后打开候选标题的完整搜索页，结果中出现多条与“女配钝感力”“网恋对象”相关的内容，以及一条《彼岸阅读日记——知乎盐选7.20》中列出的“钝力感女配网恋后”条目，说明该题材在公开知乎搜索中可见。然而，原始绑定词“OPC 集成验证”仍未命中候选内容；**内容可见性不能代替外部 `plan_id` 回执或计划状态回填**。

## 批量创建验证

在用户授权下，系统以单条合法中文词“钝感力女配网恋后”提交了知乎批量推广计划测试。开放平台成功返回 `batch_task_id`：`2071993851414327764`，已安全写入本地 `zhihu_batch_tasks` 记录与活动日志。首次结果文件回查因开放平台连接超时未完成；在任务处理后重新进行只读回查，接口成功返回结果文件，但前 50 行解析摘要为空（`[]`）。这表明批量任务已被平台受理并可回查，但当前结果文件尚未包含可显示的行级审核或导入结果。

平台的“知乎推广”分组工作台现直接展示批量任务数量、`batch_task_id`、中文状态文案、受限结果摘要与“只读回查”操作。面板仅呈现前两行且最多 180 个字符的结果摘要，不保存结果文件的原始字节。

在获得额外异步处理时间后，系统再次只读回查同一 `batch_task_id`，结果摘要仍为空数组。因此当前只能确认批量任务**已被开放平台受理并可回查**；尚不能根据结果文件确认具体条目已审核通过、导入成功或被拒绝。

由于所提供的 OpenAPI 文档未定义批量任务状态查询接口，且用户侧也没有可用的管理入口，平台将该任务固定呈现为“已受理，待结果文件回填”。后续仅在用户获得新的结果文件或主动触发只读回查时更新，不会无依据轮询、更改外部状态或重复提交。
