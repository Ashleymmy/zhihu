{
  "name": "zhihu-koc-platform",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "dev:admin": "turbo run dev --filter=platform-admin",
    "dev:leader": "turbo run dev --filter=platform-leader",
    "dev:creator": "turbo run dev --filter=platform-creator",
    "build": "turbo run build",
    "build:prod": "pnpm -r build && npm --prefix server ci && npm --prefix server run build",
    "start": "npm --prefix server run migrate:prod && npm --prefix server run start",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "test:unit": "turbo run test:unit",
    "test:e2e": "turbo run test:e2e",
    "quality": "pnpm typecheck && pnpm lint && pnpm test:unit",
    "verify:opc": "npm --prefix server run typecheck && npm --prefix server run build:core && pnpm typecheck && pnpm build && npm --prefix server run test:opc"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.4.0"
  }
}
